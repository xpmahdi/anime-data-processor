# 🎌 Anime Data Processor  
A mini functional-style Python project for processing and analyzing anime datasets.

This project demonstrates **Functional Programming (FP)** concepts in Python — including  
`lambda` functions, **higher-order functions**, and **function builders** — combined with simple data analysis techniques, JSON persistence, and a clean CLI interface.

---

## 🚀 Features

### 🔹 Functional Programming Concepts
- Lambda functions  
- Higher-order functions (`map`, `filter`, `sorted`, `reduce`)  
- Function builders (`genre_filter_builder`, `score_filter_builder`)  
- Pure functions & reusable processors  

### 🔹 Data Processing Tools
- Filter anime by genre  
- Filter by minimum score  
- Sort by score or episode count  
- Extract `(name, score)` pairs  
- Compute total episodes  
- Add new anime entries  

### 🔹 JSON Storage
Data is stored in `data/animes.json`, allowing persistent updates across runs.

### 🔹 CLI (Command Line Interface)
A simple menu-driven interface for interacting with the anime dataset.

---
